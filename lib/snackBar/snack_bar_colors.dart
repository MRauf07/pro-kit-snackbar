import 'package:flutter/cupertino.dart';

/// Default colors used when the `color` parameter is null in `NotificationType`.
class SnackBarColors {
  /// Blue color for help notifications.
  static const Color help = Color(0xff3282B8);

  /// Red color for failure notifications.
  static const Color failure = Color(0xffc72c41);

  /// Green color for success notifications.
  static const Color success = Color(0xff2D6A4F);

  /// Yellow color for warning notifications.
  static const Color warning = Color(0xffFCA652);
}
